# React Native / Expo need a flat node_modules so that transitive packages
# (expo-asset, expo-font, etc.) are resolvable by the native release bundler.
# Without this, EAS Build fails with "The required package ... cannot be found".
node-linker=hoisted
