node_modules/
.env
__pycache__/
*.pyc
.pytest_cache/
dist/
build/
.next/
pgdata/
*.db
.venv/
venv/
spec_extracted.txt
scripts/dev-tokens.txt
*.docx

# Secrets and keys — never commit
*.pem
.env.local
.env.*.local

# Local tooling / agent state
.claude/
.superpowers/
.playwright-mcp/
.expo/
*.tsbuildinfo
next-env.d.ts

# Stray marketing/build artifacts (canonical site lives in dist/)
*.zip
*.png
*.jpg
*.pdf
dwaar-ai-*.html

# Built mobile APKs — distributed via EAS / dwaarai.in/install, not committed
apk-builds/
*.apk
# Stray empty Expo config accidentally created at repo root (real config lives in apps/*/app.json)
/app.json
