paho-mqtt==1.6.1
requests==2.31.0
schedule==1.2.1

# ZKTeco C3-100 access panel — pure-Python, cross-platform (Windows edge PC).
# Used by edge/c3_controller.py (the PULL path). The default/production path is
# now the PUSH protocol (edge/c3_push_controller.py), which CAN write cards; this
# PULL lib remains for the non-push path but does NOT write the user/card table.
zkaccess-c3==0.0.15

# Legacy serial/SPI readers (edge/uhf_reader.py, edge/rfid_reader.py) are NOT
# used in the C3 architecture (readers wire into the C3 via Wiegand). Install
# these only if reviving the direct-to-host reader path:
#   pyserial==3.5      # UHF FASTag reader
#   mfrc522==0.0.7     # RC522 RFID reader
